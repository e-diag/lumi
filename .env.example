# ============================================================
# FreeWay VPN — переменные окружения
# Скопируй в .env и заполни реальными значениями
# ============================================================

# Публичный URL API (для генерации subscription URL: GET /sub/{token})
BASE_URL=https://api.freeway-vpn.ru

# PostgreSQL DSN (локально: localhost; в docker-compose: хост postgres)
DATABASE_DSN=postgres://freeway:password@localhost:5432/freeway?sslmode=disable
# DATABASE_DSN=postgres://freeway:password@postgres:5432/freeway?sslmode=disable

# Секрет для подписи JWT-токенов (мин. 32 символа)
JWT_SECRET=change-me-to-a-secure-random-string-32chars

# Токен Telegram-бота (от @BotFather)
TELEGRAM_BOT_TOKEN=1234567890:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA

# Имя бота без @ (для реферальных ссылок t.me/...)
TELEGRAM_BOT_USERNAME=FreeWayVPNBot

# ID менеджеров в Telegram (через запятую, для /manager и админ-панели в боте)
TELEGRAM_ADMIN_IDS=123456789,987654321

# Ссылки на клиенты (кнопки «Скачать»: Happ = iOS, v2RayTun = Android)
TELEGRAM_APP_URL_IOS=https://apps.apple.com/app/id0000000000
TELEGRAM_APP_URL_ANDROID=https://play.google.com/store/apps/details?id=com.v2raytun.android

# Поддержка (показывается в боте в разделе «Поддержка»)
TELEGRAM_SUPPORT_URL=https://t.me/your_support

# 3x-ui — веб-панель (провижининг клиентов). Base URL = корень панели с учётом URI Path в настройках 3x-ui.
XUI_BASE_URL=https://panel.example.com:2053
XUI_USERNAME=admin
XUI_PASSWORD=your-panel-password
# ID inbound в панели (число из списка Inbounds). Перекрывает inbound_id в config.yaml, если задано.
XUI_INBOUND_ID=1

# Публичный URL subscription-сервера 3x-ui (часто отдельный порт, см. настройки «Subscription» в панели)
# Пример: https://sub.example.com:2096 — без завершающего /
XUI_PUBLIC_SUBSCRIPTION_BASE_URL=https://sub.example.com:2096
# Сегмент пути перед subId (по умолчанию в панели «sub»)
XUI_SUBSCRIPTION_PATH=sub

# ЮKassa (для cmd/api обязательны; для cmd/bot можно оставить пустым — бот выдаёт ключи и триал без оплаты в чате)
YOOKASSA_SHOP_ID=your-shop-id
YOOKASSA_SECRET_KEY=your-secret-key

# EU нода (опционально, для fallback-генерации VLESS без 3x-ui subscription)
NODE_EU_HOST=eu.freeway-vpn.ru
NODE_EU_PUBLIC_KEY=your-eu-node-public-key
NODE_EU_SHORT_ID=your-eu-short-id

# USA нода
NODE_USA_HOST=usa.freeway-vpn.ru
NODE_USA_PUBLIC_KEY=your-usa-node-public-key
NODE_USA_SHORT_ID=your-usa-short-id

# CDN нода
NODE_CDN_HOST=cdn.freeway-vpn.ru
NODE_CDN_SNI=cdn.freeway-vpn.ru

# Веб-панель менеджера
ADMIN_WEB_TOKEN=change-me-admin-token
